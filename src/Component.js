class Component {
    // Component is the parent
    constructor(props) {
        this.props = props;
    }

    render() {
        // Child objects define their own render() methods
    }
}

export default Component;
